# Day 5 项目卡 · 助手 v2.0「历史记录管理器」

> 日期：Day 5　预计耗时：2～3 小时（含卡住时间）
> 文件：把 5.py 复制一份，另存为 `6.py`，在 6.py 上改
> 铁律：**不看旧代码抄、不让 AI 写、先自己憋 30 分钟**

---

## 一、一句话目标

把你 5.py 里的历史记录，从「只能加」升级成「**能删一条、能全清空**」。

真正的难点不在删除，在于：**删完之后关掉程序再打开，被删的东西不能回来。**

---

## 二、先看成品长什么样（终端实录）

假如此前算过 3 条，运行 6.py 后：

```
你好,我叫算数小能手,你叫什么名字 叶佳俊
你好,叶佳俊,请输入任意键进入菜单
1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 3

===== 计算历史 =====
第1题：2.0 + 3.0 = 5.0
第2题：10.0 / 4.0 = 2.5
第3题：7.0 - 9.0 = -2.0
共 3 条记录
按任意键返回菜单

1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 4
请输入要删除的编号（当前共 3 条） 2
已删除：10.0 / 4.0 = 2.5
还剩 2 条记录
按任意键返回菜单

1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 3

===== 计算历史 =====
第1题：2.0 + 3.0 = 5.0
第2题：7.0 - 9.0 = -2.0
共 2 条记录
按任意键返回菜单

1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 6
再见！
```

**重新运行程序**（关掉再打开），选 3：

```
===== 计算历史 =====
第1题：2.0 + 3.0 = 5.0
第2题：7.0 - 9.0 = -2.0
共 2 条记录
```

→ 被删的第 2 题**没有复活**。这一条是整个项目的胜负手。

再演示清空：

```
1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 5
确定要清空全部 2 条记录吗？输入 y 确认，其他任意键取消 n
已取消，什么都没删
按任意键返回菜单

1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 5
确定要清空全部 2 条记录吗？输入 y 确认，其他任意键取消 y
已清空全部记录
按任意键返回菜单

1.算数,2.打招呼,3.历史,4.删除一条,5.清空全部,6.退出 3
还没有计算记录
按任意键返回菜单
```

---

## 三、需求清单

### 必做 1：补上分隔线
选 3 看历史时，第一行先打印 `===== 计算历史 =====`。
（这是昨天漏掉的，今天补上。）

### 必做 2：新增菜单项「4.删除一条」
- 选 4 之后，让用户输入要删的**编号**（第几条）
- 删掉那一条，屏幕上告诉用户删的是哪条内容
- 删完立刻告诉用户还剩几条

### 必做 3：新增菜单项「5.清空全部」
- 选 5 之后必须**二次确认**：输入 `y` 才真删，输入其他任何东西都取消
- 确认后清空，提示已清空

### 必做 4：菜单编号重排
原来的「4.退出」变成「6.退出」，提示语要跟着改。

### 挑战（加分，不做也能交）：删完/清空后，立刻自动显示一遍剩下的历史

---

## 四、动手之前先答这 3 个问题（用中文写，不许写代码）

这是今天**最重要的一段**，答不出来就别开写，开了也白写。

**问 1.**
你在程序里把 `history` 里的第 2 条删掉了。
此时 `history.txt` 这个文件里，那一行会**自己消失**吗？为什么？

**问 2.**
你现在的 5.py 是每算完一题就用 `"a"` 模式**追加**写一行。
追加模式能"删除"某一行吗？如果不能，那你要让文件和列表保持一致，必须怎么做？
（提示：想想 `"w"` 模式是干什么的）

**问 3.**
如果 `history` 已经被清空成 `[]`，你用 `"w"` 模式写回文件的时候，写进去的应该是什么？
（提示：没有内容的时候，`"\n".join(history)` 是个什么玩意儿）

---

## 五、验收标准（10 组测试，全绿才算过）

自己在终端里一组一组跑，跑完在后面打勾。**别只看代码对不对，要真的跑。**

| # | 操作 | 期望结果 | 通过 |
|---|---|---|---|
| 1 | 历史为空时选 3 | 显示「还没有计算记录」，**不显示分隔线**或显示了也行，但不许报错 | ☐ |
| 2 | 历史为空时选 4（删除） | 提示没有记录可删，不崩、不报错 | ☐ |
| 3 | 历史为空时选 5（清空） | 同上，不崩 | ☐ |
| 4 | 算 `2+3`、`10/4`、`7-9` 三条后选 3 | 有分隔线，编号 1/2/3，最后一行「共 3 条记录」 | ☐ |
| 5 | 选 4，输入 `2` | 提示删掉的是 `10.0 / 4.0 = 2.5`，并告诉还剩 2 条 | ☐ |
| 6 | 紧接着选 3 | 只剩 2 条，**编号重新从 1 开始**（第1题、第2题） | ☐ |
| 7 | **关掉程序重新打开**，选 3 | 还是只剩 2 条，被删的没复活 ← **胜负手** | ☐ |
| 8 | 选 4，输入 `99` | 提示「没有第 99 条」，不崩 | ☐ |
| 9 | 选 4，输入 `abc` | 提示请输入数字，不崩（try/except ValueError） | ☐ |
| 10 | 选 4，输入 `0` 或 `-1` | 提示编号不合法，不崩，也不会误删 | ☐ |
| 11 | 选 5，输入 `n` | 提示已取消，历史一条不少 | ☐ |
| 12 | 选 5，输入 `y`，再选 3 | 「还没有计算记录」，且**关掉重开**后依然为空 | ☐ |
| 13 | 菜单输入 `8` | 「没有这个选项」，不崩 | ☐ |
| 14 | 全部删空之后再算一题，选 3 | 只剩刚才那 1 条，前面被删的没回来 | ☐ |

---

## 六、卡住了？15 分钟一级，逐级打开

### 提示 1（关于"怎么从列表里删一条"）
你现在**没学过** `del` 和 `pop`，我也不许你用。
但你有别的办法：新建一个空列表，然后用你昨天写过的「for 循环 + 计数器」，
把**不想删的那几条**一个一个装进新列表，跳过要删的那条。
最后让 `history` 指向这个新列表。

### 提示 2（关于"怎么让文件跟着变"）
列表改了，文件不会自己变。
你必须在**每一次删除/清空动作完成后**，用 `"w"` 模式把**整个 history 重新写一遍**进文件。
`"a"` 是追加 —— 追加只能往后糊，糊不出"删除"的效果。`"w"` 是推倒重写。

### 提示 3（关于"写回文件的内容"）
文件里每一行是一条记录，行和行之间是换行。
你昨天学过 `splitlines()` 是把文件**撕成**列表，
那反过来把列表**粘成**一整块文本该用什么？（昨天也讲过，两个字 + 一个 `join`）
粘完之后记得最后补一个换行，不然下次再追加会粘到同一行上。

---

## 七、交作业

1. `6.py` 放在 `C:\Users\叶佳俊\Documents\trae_projects\20260904\`
2. 把 14 组测试里**第 7 组和第 12 组**的结果抄给我（或截图）—— 这两组过了，我今天就算你赢
3. 提交 Git：
   ```
   git add 6.py
   git commit -m "Day5: 助手v2.0，历史可删除可清空，文件与列表同步"
   git push
   ```
4. 把第四节那 3 个问题**用你自己的话**回答给我（这是今天的复盘，不许抄）

---

## 八、自学加餐 · 5 个新武器（替代搞破坏，想吃几个吃几个）

按你自己节奏来，不要求一次吃完。但下面 3 条是死线：

> **死线 1**：我只给搜索词，**不给你讲原理**。自己查（菜鸟教程 / 官方文档 / 问 Trae 都行）。
> **死线 2**：每个武器必须**手敲 3 行以上代码并跑通**。看懂了 ≠ 会了，你以前吃亏就吃在这。
> **死线 3**：**至少挑 1 个缝进 6.py**。不落进项目里的新知识，三天后一定忘。

---

### 武器 1：`import` + 时间 ⭐ 建议第一个学

- **解决什么**：现在每条记录只有 `2.0 + 3.0 = 5.0`，不知道什么时候算的。加上时间，你的助手才像个真东西。
- **为什么现在学**：这是你人生第一次 `import`（用别人写好的代码）。而且它**就是「智管贷后」里"操作留痕"的雏形**——2026 新规下留痕即合规，这是你项目的立身之本。
- **搜索词**：`python import datetime 当前时间 格式化 strftime`
- **最小验证**：手敲代码，打印出 `2026-09-08 07:25` 这种格式的时间
- **缝进 6.py**：把 record 改成 `f"[{时间}] {a} {op} {b} = {result}"`
- **预期卡点**：你会查到 `datetime.datetime.now()` 这种套娃写法，别慌，**先照抄跑通，再回头想为什么套两层**

### 武器 2：f-string 控制小数位

- **解决什么**：现在 `10 / 3` 出来 `3.3333333333333335`，又丑又不可控。
- **搜索词**：`python f-string 保留两位小数 :.2f`
- **最小验证**：让 `10 / 3` 打印成 `3.33`
- **缝进 6.py**：所有算数结果用这个格式输出
- **隐藏坑**：格式化之后它变成**字符串**了，不能再拿去加减乘除。想清楚该在哪一步格式化

### 武器 3：`def` 函数 ⭐ 语言杠杆最大的一个

- **解决什么**：你现在 6.py 里「按任意键返回菜单」写了五六遍，显示历史的代码也快重复了。函数 = 给一段代码起个名字，以后喊名字就行。
- **搜索词**：`python def 函数 定义 调用 参数 返回值`
- **最小验证**：写一个函数，喊它一次就打印一次分隔线
- **缝进 6.py**：把「显示历史」那段抽成函数（选 3 用一次，删除完回显再用一次，正好解决重复）
- **为什么重要**：这是你从"写代码"到"组织代码"的分界线。10 月的 FastAPI 里，满屏都是函数

### 武器 4：`.split()` 拆字符串

- **解决什么**：一条记录现在是整一坨 `"2.0 + 3.0 = 5.0"`，想单独把结果 `5.0` 抠出来很别扭。split 能按空格把它切成好几块。
- **搜索词**：`python split 字符串 分割 空格`
- **最小验证**：把 `"2.0 + 3.0 = 5.0"` 切成 5 块，打印每一块
- **直连明天**：切开了，明天就能装进字典

### 武器 5：`dict` 字典（明天的正式内容，提前偷跑）

- **解决什么**：split 切出来是一堆散块，字典能让它们**有名字**。
- **搜索词**：`python 字典 dict 键值对 增删改查`
- **最小验证**：建一个 `{"题目": "2 + 3", "结果": 5.0}`，打印出来 → 改掉结果 → 再加一个"时间"
- **直连项目**：这就是「智管贷后」里一条客户记录的样子（姓名 / 逾期天数 / 催收次数 / 合规标记）

---

### Bug 账本（搞破坏的低成本替代，不占时间）

不单独花 20 分钟搞破坏了。但以后**每次撞到报错**，在这儿记一行，30 秒：

| 报错 / 现象 | 我以为的原因 | 真正的原因 |
|---|---|---|
| `IndexError: pop index out of range` | | 编号从 1 开始，位置从 0 开始，删最后一条时 pop(n) 越界 |

你单日出过 6 次缩进错位、改完不跑全路径测试就提交。这账本不是给我看的，是给你自己看的——**同一个坑踩第二次，说明上次没真懂。**

---

## 十、08:22 批改记录（导师填）

### 已通过 ✅
- `"w"` 模式 + for 循环写回整个列表 —— 今天的胜负手拿下（空列表时循环不跑，文件自动清空，无需判断）
- `if 1 <= n <= len(history)` 链式比较 —— 一条语句拦住 0 / -1 / 99 三种非法输入，写得漂亮
- 选 3 / 4 / 5 三处空历史守卫齐全
- `try / except ValueError` 用于 int 转换，位置正确
- 菜单已重排为 1~6

### 待补 ❌
1. **清空必须二次确认**（输 `y` 才真删）—— 目前选 5 直接清空，第 11、12 组测试挂
2. 选 3 缺 `===== 计算历史 =====` 分隔线
3. 删完只说"已删除第 N 题"，没显示删掉的内容、没说还剩几条

### 违规待答辩 ⚠️
- 用了未教的 `history.pop(n - 1)` 和 `history.clear()`（搬来的）
- 答辩题：**`pop(n - 1)` 为什么减 1？**
- 用户 08:22 作答："不减，就会只有3条记录，显示是4条" → **方向对（知道会错位），表述不准**
- 判定：**放行**，但需用准确的话复述（见对话）
- 完整答案：不减 1 有两种后果 —— ① 删中间某条 → 静默删错（要删 B 结果删了 C，不报错）② 删最后一条 → `IndexError: pop index out of range` 崩溃

### 流程提醒
- **别改 5.py**：那是 Day 4 纯手写版且已提交 git，是给面试官看的成长证据。另存 6.py 再改。

---

## 九、明天（Day 6）预习

今天你会发现一件事：**一条记录只有一行文本**，想单独改它的某一部分（比如只改结果）很别扭。

明天学 **字典 dict**，它长这样：

```python
{"题目": "2 + 3", "结果": 5.0, "时间": "09-08 10:22"}
```

一个东西可以有好几个名字对着的属性，不再是一整坨字符串。
这是通往「智管贷后」里那条客户记录（姓名/逾期天数/催收次数/合规标记）的第一步。

今晚睡前想一想：**如果一条历史记录是个"东西"，你会给它拆出哪几个属性？**
