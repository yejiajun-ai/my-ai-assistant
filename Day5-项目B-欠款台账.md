# Day 5 · 项目 B「欠款台账 v1」

> 立项原因：6.py 里的 `pop` / `clear` 是搬来的。换个全新项目，抄不到旧代码，才能验出真本事。  
> 文件：新建 `7.py`，**不许打开 5.py / 6.py**  
> 数据文件：`debt.txt`

---

## 一、一句话目标

写一个能**加、看、删、清空、统计**的欠款台账，数据存硬盘，关掉程序不丢。

选这个题材的理由：这是你熟的行业，而且**它就是「智管贷后」的第一块积木**——今天存的一条欠款记录，将来会长成带合规标记的客户档案。

---

## 二、成品长什么样（终端实录）

```
===== 欠款台账 v1 =====
1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出
请选择 1

客户姓名 张三
欠款金额 5000
已添加：张三  欠款 5000 元
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 1
客户姓名 李四
欠款金额 3200
已添加：李四  欠款 3200 元
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 2

===== 欠款明细 =====
第1条：张三  5000 元
第2条：李四  3200 元
共 2 条
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 5

===== 统计 =====
客户数：2 人
欠款总额：8200 元
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 3
请输入要删除的编号（当前共 2 条） 1
已删除：张三 5000 元
还剩 1 条
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 4
确定要清空全部 1 条记录吗？输入 y 确认，其他任意键取消 n
已取消，什么都没删
按任意键返回菜单

1.添加客户  2.查看全部  3.删除一条  4.清空全部  5.统计  6.退出 6
再见！
```

**关掉重新打开**，选 2 → 李四那条还在，被删的张三没回来。

---

## 三、需求清单

### 存储格式（先想清楚这个再动手）

一条记录存成这样：

```
张三|5000
李四|3200
```

**为什么用 `|` 隔开，不用空格？**  
因为姓名可能有空格（"阿卜杜 热合曼"），用空格分隔会把它劈成两半。`|` 这种符号正文里基本不会出现，当分隔符更稳。

→ 这就是"设计数据格式"。真实项目里这步想错了，后面全是坑。

### 功能

| # | 功能   | 要求                                   |
| - | ---- | ------------------------------------ |
| 1 | 添加客户 | 输姓名 + 金额，存进文件和列表                     |
| 2 | 查看全部 | 带 `===== 欠款明细 =====` 分隔线、带编号、末尾显示共几条 |
| 3 | 删除一条 | 输编号，删掉，**显示删的是谁 + 还剩几条**             |
| 4 | 清空全部 | **二次确认**，输 `y` 才真删                   |
| 5 | 统计   | 显示客户数 + **欠款总额**                     |
| 6 | 退出   | 干干净净退出                               |

---

## 四、三个核心考点（就是来考你的）

### 考点 1：删完必须写回文件（老考点，换个项目再考一次）

跟 6.py 一样：列表删了，文件不会自己变。必须 `"w"` 模式 + for 循环重写。  
**这是今天唯一的胜负手。**

### 考点 2：统计总额（新考点）

现在 `history` 里每条是 `"张三|5000"` 这种字符串。要算总额，你得：

1. 把 `5000` 从 `"张三|5000"` 里**抠出来**（今天加餐武器 4 的 `split()` 派上用场了）
2. 抠出来的是**字符串** `"5000"`，得转成数字才能加
3. 累加器：循环外面 `total = 0`，里面 `total = total + 金额`

> **预告一个你必踩的坑**：如果忘了第 2 步，你会看到总额变成一串怪东西。  
> 到时候别急着改，先想想"`+` 号两边都是字符串的时候，Python 在干什么"。

### 考点 3：二次确认 + 分隔线（6.py 欠的，这次必须做对）

- 清空前必须问一句，输 `y` 才动手
-  

---

## 五、铁律（这次是来证明自己的）

> **① 不许打开 5.py / 6.py。** 新建 `7.py` 从零写。  
> **② 前 30 分钟不许问 Trae。** 卡住就自己在纸上画流程。  
> **③ 第一遍禁用 `pop` / `clear` / `del`。** 只用 `append` + 新箱子写法（就是早上那个演算表格）。

第 ③ 条是重点：**你得先证明用最土的语法你能写出来，再去用高级语法。** 反过来就是空中楼阁。

---

## 六、两遍写法（写完第一遍再做第二遍）

**第一遍**：新箱子写法

- 新建空列表 → for 循环 + 计数器 → 不等于就 append → 最后让 `history` 指向新列表

**第二遍**：把删除那段换成 `pop`

- `pop(n - 1)`，感受一下省了几行

**然后回答我**：两种写法，你喜欢哪个？为什么？  
（提示：想想数据量大的时候——1 万条记录，一个要重建整个列表，一个直接挖掉一个洞。）

---

## 七、验收标准（12 组，全绿才算过）

| #  | 操作                    | 期望结果                     | 通过 |
| -- | --------------------- | ------------------------ | -- |
| 1  | 空台账选 2                | 提示还没有记录，不崩               | ☐  |
| 2  | 空台账选 3                | 提示没有可删的，不崩               | ☐  |
| 3  | 空台账选 4                | 提示没有可清空的，不崩              | ☐  |
| 4  | 加张三 5000、李四 3200 后选 2 | 有分隔线、编号 1/2、末尾「共 2 条」    | ☐  |
| 5  | 选 5（统计）               | 客户数 2 人，总额 **8200** 元    | ☐  |
| 6  | 选 3 输入 `1`            | 显示「已删除：张三 5000 元」，还剩 1 条 | ☐  |
| 7  | 紧接着选 2                | 只剩李四，**编号从 1 重新开始**      | ☐  |
| 8  | **关掉重开**，选 2          | 还是只剩李四，张三没复活 ← **胜负手**   | ☐  |
| 9  | **关掉重开**，选 5          | 总额变成 **3200**，不是 8200    | ☐  |
| 10 | 选 3 输入 `99`           | 提示没有第 99 条，不崩            | ☐  |
| 11 | 选 3 输入 `abc`          | 提示请输入数字，不崩               | ☐  |
| 12 | 选 3 输入 `0` 或 `-3`     | 提示编号不合法，不崩，不误删           | ☐  |
| 13 | 选 4 输入 `n`            | 已取消，一条没少                 | ☐  |
| 14 | 选 4 输入 `y`，重开后选 2     | 空的，且 `debt.txt` 是 0 字节   | ☐  |

---

## 八、加分题（做完再说，别一开始就贪）

1. 金额输入负数时拒绝（欠款不能是负的）
2. 姓名输入为空时拒绝（不能存无名氏）
3. 添加时如果同名客户已存在，提示一下

---

## 九、交作业

1. `7.py` 放在 `C:\Users\叶佳俊\Documents\trae_projects\20260904\`
2. 第 **8** 组和第 **14** 组的结果给我（这两组过了就盖章）
3. 第六节那个问题：两种写法你喜欢哪个，为什么
4. 提交 Git：
   ```
   git add 7.py
   git commit -m "Day5: 欠款台账v1，从零手写，增删统计+文件同步"
   git push
   ```
5. **回头花 10 分钟把 6.py 欠的三样补上**（二次确认 / 分隔线 / 删完显示内容），也提交掉

---

## 十、为什么让你写这个而不是再写个计算器

因为计算器你已经"背下来"了——结构熟、套路熟，写再快也是在默写。

**欠款台账有三个地方你没法默写：**

- 一条记录有**两个字段**（姓名 + 金额）→ 得设计存储格式
- 有**统计** → 得把字符串掰开、转成数字、累加
- 有 `|` 分隔符 → 得用 `split` 才能取出金额

这三处会逼着你真的想。写完之后你再看明天的字典，会有种"啊原来昨天那个别扭是有解的"的感觉。

---

## 十一、10:55 批改记录（导师填）

### 自动化验收结果（subprocess 喂输入，四轮全跑）

| 轮次           | 结果                    |
| ------------ | --------------------- |
| 第一轮 加/看/统计/删 | 统计 8200.0 ✅ 没踩字符串拼接坑  |
| 第二轮 关掉重开     | 只剩李四，张三没复活 ✅ **胜负手过** |
| 第三轮 清空       | 能清空 ⚠️ 但没二次确认         |
| 第四轮 重开       | 仍为空 ✅ 第 14 组过         |

### 通过 ✅

- 文件读写同步：`"a"` 追加添加 / `"w"` 重写删除，两侧都对
- 统计：`split("|")` → `float()` → 累加器，全对，**绕过了字符串拼接坑**
- 越界守卫 `1 <= m <= len(debt)`，提示还带范围
- `while True + try/except` 金额重试
- 删除支持输入 `q` 返回（自己想到的，加分）

### 待补 ❌

1. 🔴 清空二次确认（**第二次欠**，必须补）
2. 🟡 查看 / 统计缺 `=====` 分隔线
3. 🟡 统计缺"客户数：N 人"
4. 🟡 删完缺"还剩几条"
5. 🟡 显示暴露分隔符：`第1条：张三|5000.0` —— 应 split 拆开再拼，`|` 和 `.0` 都不该给用户看

### 答辩记录

- `pop(n - 1)`：08:22 已答辩通过（方向对，表述已修正）
- `debt.clear()`：10:55 答辩 **通过**
  - 用户答："clear 就是清空列表内存，后面一行代码就把清空这个行为写进硬盘文件"
  - 判定：✅ 正确，且抓住了"内存 vs 硬盘"这个核心框架

### 本次核心反馈（比补功能更重要）

**"你的问题不是不会写，是不看需求清单。"**  
6.py 和 7.py 漏的是同一类东西——全是展示层细节，没有一个是核心功能。心智是"跑通就行"，工程要求是"需求逐条对钩"。这是爱好者和工程师的分水岭，也是面试官唯一能一眼看穿的。  
→ **新规矩：交作业前自己对着验收表逐条打钩，钩打完再来找我。**

### 待办

- 用中文写出「新箱子版删除」步骤（铁律③的替代证明，未答）

---

## 十二、导师示范：「新箱子版删除」用中文怎么说

（用户 18:02 反问"你能表述清楚吗"，先给示范，再要求他用**自己的话**复述）

### 场景

`debt` 里有 3 条，用户说"删第 2 条"。
**限制**：不许在原地挖洞（禁用 `pop` / `del`），只能整体搬家。

### 第一步：准备两样东西

1. 拿一个**空的新箱子**
2. 拿一个**计数器**，从 0 开始（发号码牌用的）

### 第二步：从旧箱子第一条开始，一条一条过

每拿到一条，做两件事：

- **先把计数器加 1**（这条是第几号）
- 问一句：**这个号码，是不是用户要删的那个号？**

### 第三步：判断这条留不留

- **不是**要删的 → 装进新箱子
- **是**要删的 → **不装，直接跳过**

跳过 = 删除。你从头到尾没动过旧箱子。

### 第四步：换箱子

循环结束，让 `debt` 指向那个新箱子。
旧箱子（还装着原来所有东西）就此作废。

### 一句话版

**挑着搬一遍，跳过不要的那个，然后用新箱子顶替旧箱子。**

### 三个角色

| 角色 | 是谁 | 作用 |
|---|---|---|
| 🥇 主角 | 新箱子 | 承载最终结果 |
| 🥈 配角 | 计数器 | 给每条发号码牌，用来比对 |
| 🥉 彩蛋 | 第四步"让 debt 指向新箱子" | **最容易忘，忘了等于白干** |

### 两个必须注意的点

1. **计数器必须先加再用** —— 第一条的号才是 1，跟屏幕上"第1条"对齐。放后面就变成 0 号，会删错。
2. **判断用"不等于"** —— 你要写的是"**留下来**"的条件，不是"删掉"的条件。方向反了会只剩要删的那条。

### 对比 pop

| | pop | 新箱子 |
|---|---|---|
| 动作 | 在旧列表上挖个洞，抽走一个 | 新建一个，跳过那个不搬 |
| 改的是 | 原列表 | 新建一个列表 |
| 一万条时 | 直接挖洞，快 | 要重建整个列表，慢 |

---

## 十三、18:02 第二次批改（7.py 已被重构为函数式）

### 变化

10:59 提交的版本从 88 行脚本变成 155 行的**函数化结构**：8 个 `def`、`if __name__ == "__main__":`、docstring、默认参数 `path=DEBT_FILE`、`return` 返回值。

### 漏项补了 3 / 4 ✅

- 🔴 清空二次确认 → **已补**（`confirm = input(...) if confirm == "y"`）
- 🟡 分隔线 → **已补**（`===== 欠款列表 =====` / `===== 欠款统计 =====`）
- 🟡 客户数 → **已补**（`客户数量：{len(debt)}`）
- 🟡 删完"还剩几条" → **仍未补**
- 🟡 显示暴露 `|` 和 `.0` → **仍未补**

### 意外惊喜（加分）

`calc_total` 里加了格式校验：

```python
parts = record.split("|")
if len(parts) < 2:
    continue
```

**跳过格式错误的行，而不是让整个程序崩掉。** 这个我没要求，是健壮性意识——正是「智管贷后」需要的（脏数据不能让系统挂）。

### 但这次跳跃必须处理 ⚠️

`def` / `__main__` / docstring / 默认参数 **都没教过**，跨度远超今天的进度。
按规矩「搬来的代码必须 24 小时内换成自己懂的写法」——**这份代码目前不是他的。**

**答辩清单（8 个函数 + 3 道必答题）：**

必答：
1. `if __name__ == "__main__":` 这一行是干嘛的？删掉会怎样？
2. `parse_amount` 为什么不直接报错，要返回 `None`？
3. `save_debt(debt)` 在程序里被调用了几次？分别在什么情况下？

选答（8 个函数逐个说清"输入什么、输出什么"）：
`load_debt` / `save_debt` / `clear_file` / `append_record` / `format_record` / `parse_amount` / `parse_index` / `calc_total` / `build_list_view` / `build_stats_view`

**答得上来 = 真学会了，盖章。答不上来 = 明天改回自己看得懂的版本。**

---

## 十四、def 速查（18:11 补，第一次学函数）

### 一句话

**函数 = 给一段代码起个名字，以后喊名字就行。**

### 洗衣机类比

| 洗衣机的概念 | 对应 Python |
|---|---|
| 按「标准洗」按钮 | **调用函数**：`save_debt(debt)` |
| 按钮背后那套程序（进水、转、排水） | **函数体**（缩进的那段） |
| 你塞进去的脏衣服 | **参数**（输入） |
| 洗完拿出来的干净衣服 | **return**（输出） |
| 你不用知道电机怎么转 | 用的人不需要懂内部实现 |

### 五个零件

| 零件 | 写法 | 作用 |
|---|---|---|
| 造按钮 | `def 名字(参数):` | 定义 |
| 按钮背后 | 缩进的代码块 | 函数体 |
| 吐结果 | `return 值` | 返回值（可以没有） |
| 按按钮 | `名字(参数)` | 调用 |
| 默认值 | `path=DEBT_FILE` | 不传就用这个 |

### ⚠️ 最容易懵的一点：定义的时候，函数体一行都不执行

跑出来的顺序：

```
① 程序开始了
② 函数定义完了 —— 注意上面那个'你好'一次都没打印出来
   你好，张三！        ← 直到调用才打印
③ 调用了一次，返回给我的东西是：2
```

**不喊它，它就永远不动**（`没人喊我()` 从头到尾没跑）。

### 两种函数

| 类型 | 特征 | 你 7.py 里的 |
|---|---|---|
| **干活型** | 执行动作，没有 return | `save_debt` `clear_file` `append_record` |
| **算数型** | 加工数据，有 return | `format_record` `parse_amount` `parse_index` `calc_total` `load_debt` `build_list_view` `build_stats_view` |

**不是所有函数都吐东西。** `save_debt` 的活是"写文件"，干完就完了，不需要 return。

### 你那 10 个函数的输入输出

| 函数 | 输入 | 输出 |
|---|---|---|
| `load_debt()` | 无 | 列表 |
| `save_debt(debt)` | 列表 | 无（去写文件了） |
| `clear_file()` | 无 | 无（去清空了） |
| `append_record(record)` | 一条记录文本 | 无（去追加了） |
| `format_record(name, amount)` | 姓名、金额 | 一条记录文本 |
| `parse_amount(text)` | 文本 | 数字 或 `None` |
| `parse_index(text, max_n)` | 文本、上限 | 编号 或 `None` |
| `calc_total(debt)` | 列表 | 总额数字 |
| `build_list_view(debt)` | 列表 | 多行文字的列表 |
| `build_stats_view(debt)` | 列表 | 多行文字的列表 |
